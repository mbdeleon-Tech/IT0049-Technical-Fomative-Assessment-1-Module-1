# Database export

This assessment version does not use a database. Customer and user records are intentionally stored as static PHP arrays in their controllers, as required by the activity instructions. A database export will be added in the next module when persistence is introduced.
